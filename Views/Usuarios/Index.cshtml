

@model IEnumerable<Crud.Models.Usuarios>


@{
    ViewData["Title"] = "Index";
}

<h1 class="text-center">Turnos pendientes</h1>

<p>
    <a  class="btn btn-secondary" asp-action="Create">Generar nuevo turno</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                Cola Asignada
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Nombre)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Apellido)
            </th>
            <th>
                Cédula
            </th>
           
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Cola)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Nombre)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Apellido)
            </td>

            <td>
                @Html.DisplayFor(modelItem => item.Cedula)
            </td>
           
            <td>
                @*<a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |*@
                @* <a asp-action="Details" asp-route-id="@item.Id">Details</a> |*@
                <form asp-action="Delete">
                    <input type="hidden" asp-for="@item.Id" />
                    <input type="submit" value="Borrar" class="btn btn-danger" />                
                </form>
            </td>
                </tr>
                }
            </tbody>


        </table>

        <div class="row text-center justify-content-md-center">
            <div class="col-sm-3">
                <div class="card ">
                    <div class="card-body">
                        <h5 class="card-title">Atendiendo a</h5>
                        <p style="font-size:30px;"><b> </b></p>
                    </div>
                </div>
            </div>
            <div class="col-sm-3">
                <div class="card">
                    <div class="card-body">
                        <h5 class="card-title">Atendiendo a</h5>
                        <p style="font-size:30px;"><b> </b></p>
                    </div>
                </div>
            </div>
        </div>



